{
	"name": "code-challenge-frontend",
	"version": "1.0.0",
	"main": "index.js",
	"engines": {
		"node": "14.x",
		"yarn": "1.x"
	},
	"repository": "git@github.com:PatrikAarnivaara/code-challenge-frontend.git",
	"author": "Patrik Aarnivaara <patrikaarnivaara@gmail.com>",
	"license": "MIT",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.17.1"
	},
	"scripts": {
		"start": "node index.js",
		"build": " GENERATE_SOURCEMAP=false cd client && yarn install && yarn run build",
		"heroku-postbuild": "cd client && yarn install && yarn run build"
	}
}
